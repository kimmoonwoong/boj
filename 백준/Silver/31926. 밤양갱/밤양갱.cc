#include <iostream>
#include <vector>
#include <set>
#include <algorithm>
#include <cmath>
#define ll long long
#define pii pair<int, int>
using namespace std;

void solve() {
	int n; cin >> n;
	cout << 10 + (int)floor(log2(n));
}

int main() {
	ios::sync_with_stdio(false); cin.tie(NULL); cout.tie(NULL);

	solve();
}